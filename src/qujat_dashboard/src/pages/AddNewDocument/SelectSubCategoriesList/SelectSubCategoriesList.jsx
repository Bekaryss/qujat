import React from "react";
import { Box } from "@mui/material";

const SelectSubCategoriesList = ({ selectedValues }) => {
  return (
    <div>
      {/* {selectedValues.map((item) => (
        <Box key={item.id}>
          <span>{item.nameKz}</span>
        </Box>
      ))} */}
    </div>
  );
};

export default SelectSubCategoriesList;
